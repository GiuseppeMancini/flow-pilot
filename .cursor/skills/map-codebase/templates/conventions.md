# Conventions

## Commands

Document the real commands from package scripts, Makefile, or docs. Adjust rows to the project.

| Command | Purpose |
|---------|---------|
| <!-- e.g. `npm run dev` --> | Start app locally |
| <!-- e.g. `npm run build` --> | Production build |
| <!-- e.g. `npm test` --> | Run tests |
| <!-- e.g. `npm run lint` --> | Linter |
| <!-- e.g. `npm run format` / `npx prettier` --> | Formatter |
| <!-- e.g. `npm run typecheck` --> | Typecheck (if applicable) |
| **Debug** | <!-- e.g. VS Code launch name, `F5`, remote debug port --> |

## Naming

<!-- Files, classes, functions, variables, database tables/columns -->

## Patterns

<!-- Error handling, logging, dependency injection, validation, etc. -->

## Code style

<!-- Rules not covered by linter/formatter config -->
